{
  "name": "rucher-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:prod": "npm run migrate:deploy && node src/index.js",
    "dev": "node --watch src/index.js",
    "build": "npx prisma generate && npm run build:frontend",
    "build:frontend": "cd ../frontend && npm install && npm run build && node -e \"require('fs').rmSync('../backend/public',{recursive:true,force:true});require('fs').mkdirSync('../backend/public',{recursive:true});require('fs').cpSync('dist','../backend/public',{recursive:true})\"",
    "migrate:deploy": "npx prisma migrate deploy",
    "seed": "node prisma/seed.js",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.3.0",
    "prisma": "^5.22.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}